{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "description": "iroha-for-session: inject the project's prior State + a save reminder at session start, proactive local recall (local BM25 (TypeScript) over the keys-only index, no LLM/network) of relevant past decisions on each prompt, and a write-time advisory before `git commit` that surfaces the Active decisions governing the change (so a silent course-reversal is caught before it lands).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bun \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.ts\"",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bun \"${CLAUDE_PLUGIN_ROOT}/hooks/recall-inject.ts\"",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "bun \"${CLAUDE_PLUGIN_ROOT}/hooks/check-inject.ts\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
