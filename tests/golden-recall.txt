# Golden set for local recall quality — the SINGLE source of truth shared by:
#   tests/recall-eval.ts   (FREE tier: hand-rolled TS BM25)
#   tests/hybrid-eval.ts   (HEAVY tier: BM25 ∪ dense -> cross-encoder rerank)
# One case per line: "<query>|<expected-page-id>". expected id "NONE" = the query MUST abstain.
# Queries are paraphrases a developer would actually type (NOT a decision's own words), so this
# measures real recall, not echo. IDs are this repo's real Decision/Session pages.
#
# RE-BASED 2026-06-28 (3rd time): the workspace was re-saved into a new container, so the prior ids
# no longer exist. These ids are the current index's three Decision rows (HEAVY実行 / 依存方針 / 保存).
# Re-label again whenever the index changes materially (a reset, or after it grows enough that
# better paraphrases exist). Lines starting with '#' and blank lines are ignored by the eval loops.
リランカーのモデルをnode spawnせずBun本体のin-processで動かす|38c822c6-938a-8178-9e2e-d6b2a9bf583d
新しい依存を足さずZodも入れず依存ゼロを維持する方針|38c822c6-938a-8157-81fb-ed6b98e64afe
Notion保存でAPIトークンを使わず本文を決定論で生成する|38d822c6-938a-81ac-be6a-cd2cf49944ab
configure nginx reverse proxy with tls termination|NONE
optimize the react component re-rendering performance|NONE
terraform provider configuration for gcp networking|NONE
おすすめの映画を教えてほしい|NONE
