# Golden set for local recall quality — consumed by tests/recall-eval.ts (hand-rolled TS BM25).
# One case per line: "<query>|<expected-page-id>". expected id "NONE" = the query MUST abstain.
# Queries are paraphrases a developer would actually type (NOT a decision's own words), so this
# measures real recall, not echo.
#
# These ids point at the FROZEN fixture corpus tests/fixtures/recall-corpus/.iroha/index.ndjson — NOT
# the live repo index. That decoupling is deliberate: a workspace re-save / re-init churns the live
# decision ids and used to red-fail CI every time (3× before this). The fixture is a snapshot, so the
# ids here are stable. Only re-label when you DELIBERATELY regenerate the fixture (see recall-eval.ts
# header), never as forced re-save fallout. The three Decision rows are HEAVY実行 / 依存方針 / 保存.
# Lines starting with '#' and blank lines are ignored by the eval loops.
リランカーのモデルをnode spawnせずBun本体のin-processで動かす|38c822c6-938a-8178-9e2e-d6b2a9bf583d
新しい依存を足さずZodも入れず依存ゼロを維持する方針|38c822c6-938a-8157-81fb-ed6b98e64afe
Notion保存でAPIトークンを使わず本文を決定論で生成する|38d822c6-938a-81ac-be6a-cd2cf49944ab
configure nginx reverse proxy with tls termination|NONE
optimize the react component re-rendering performance|NONE
terraform provider configuration for gcp networking|NONE
おすすめの映画を教えてほしい|NONE
